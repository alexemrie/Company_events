<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>

  <% unless @company.new_record? %>
    <%= link_to "Delete", company_path(@company), method: "delete", data: {confirm: "Are you sure you want to delete this company?"} %>
  <% end %>

<% end %>
